<!DOCTYPE html>
<html>
    <head>
    </head>
    <body>
        <% if @user.id == current_user.id %>
            <form action= '/valueplus/myCareer' method='post'>
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <p><input type="text" name="content"></p>
                <button type ='submit' class="waves-effect waves-light btn-large red accent-1 intro">submit</button>
            </form>
        <% end %>
        <% @myCareer.each do |c| %>
            <p><%= c.content %></p>
            <% if @user.id == current_user.id %>
                <p><a href="/valueplus/myCareerEdit/<%= c.id %>">수정</a>
                <a href="/valueplus/myCareerDestroy/<%= c.id %>">삭제</a></p>
            <% end %>    
        <% end %>
    </body>
</html>